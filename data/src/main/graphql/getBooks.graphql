query getBooks($categoryId:String!){
    getBooks(category:$categoryId) {
        numberOfPages
        books {
            name
            _id,
            cover{
                path
            },
            author{
                _id,
                name
            },
            avgRate
        }
    }
}